query getAllCampaigns {
  campaignList(order_by: {creationDate: desc}) {
    title
    owner {
      address
      name
    }
    description
    creationDate
    category
    closed
    donated
    goal
    version
    contract
    ascii
  }
}
